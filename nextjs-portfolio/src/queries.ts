export const WORKQUERY = `*[_type == "work" && language == $language]{ _id, company, position,"imageURL": image.asset->url, location, startDate, endDate, stillWorksHere, description, nonIT, "_translations": *[_type == "translation.metadata" && references(^._id)].translations[].value->{company, position,"imageURL": image.asset->url, location, startDate, endDate, stillWorksHere, description, nonIT}, } | order(startDate desc)`;
export const EDUCATIONQUERY = `*[_type == "education" && language == $language]{ _id, institution, degree, location, startDate, endDate, description, "_translations": *[_type == "translation.metadata" && references(^._id)].translations[].value->{institution, degree, location, startDate, endDate, description}, } | order(startDate desc)`;
export const PROJECTQUERY = `*[_type == "project" && language == $language]{ _id, title, description, startDate, endDate, "imageURL": image.asset->url, "githubURL": githubURL, "demoURL": demoURL, "_translations": *[_type == "translation.metadata" && references(^._id)].translations[].value->{title, description, startDate, endDate, githubURL, demoURL}, } | order(startDate desc)`;